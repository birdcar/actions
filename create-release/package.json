{
  "name": "@birdcar/create-release",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Automatically creates a GitHub release from a new tag, including changelog notes",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outfile=dist/index.js --target=node --minify",
    "test": "bun test"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "keep-a-changelog": "^2.5.0"
  }
}
