{
  "name": "@birdcar/deploy-to-heroku",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Deploy applications to Heroku using GitHub Actions",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outfile=dist/index.js --target=node --minify",
    "test": "bun test"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1"
  }
}
