{
  "name": "@birdcar/poetry-export",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Generate requirements.txt from poetry.lock for PaaS deployments",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outfile=dist/index.js --target=node --minify",
    "test": "bun test"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1"
  }
}
